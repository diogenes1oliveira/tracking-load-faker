language: python
python: "3.7"
dist: xenial

cache:
  pip: true

install:
  - pip install --upgrade pip
  - pip install poetry codecov
  - poetry install -v

script:
  - flake8
  - pytest --cov -s

after_success:
  - codecov
